/**
 * CSS classes, other CSS classes defined in json layout in first element of key array
 */

// Класс для подсветки текущего символа, который нужно напечатать
export const highlighted = 'highlighted';

// Класс для текста, который еще предстоит напечатать
export const type = 'type';

// Класс для уже напечатанного текста
export const typed = 'typed';

// Класс для отображения ошибок при наборе текста (error text color)
export const etext = 'e-text-c';

// Класс для строки клавиш на виртуальной клавиатуре
export const keysrow = 'keys-row';

// Класс для разделителя между группами клавиш
export const delimiter = 'keys-delimiter';

// Класс для отдельного ряда клавиш
export const keyrow = 'key-row';

// Класс для ячейки, содержащей клавишу
export const keycell = 'key-cell';

// Класс для специальных клавиш (Enter, Shift, Backspace и т.д.)
export const keyspecial = 'key-special';

// Суффикс класса для обозначения нажатого состояния клавиши
export const fingerdown = '-d';

// Идентификатор для клавиши обратного слэша
export const backslash = 'Backslash';

// Базовый класс для всех клавиш
export const key = 'key';
